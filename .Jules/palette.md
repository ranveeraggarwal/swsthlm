## 2024-06-29 - Empty State Clear Filters Button
**Learning:** Adding a "Clear all filters" button directly within an empty state ("0 results") is a highly effective, non-intrusive micro-UX pattern that saves users from hunting for a global reset button.
**Action:** Always include actionable recovery paths (like reset buttons) within empty states caused by user filtering or searching.
